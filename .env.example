# Environment variables required to run the project
# IntelliJ: Edit Configurations → Environment variables
# Terminal (Linux/Mac): export VARIABLE=value
# PowerShell: $env:VARIABLE="value"

# PostgreSQL database password
DB_PASSWORD=

# JWT signing key — generate with: openssl rand -base64 32
JWT_SECRET=